// Minimal JS skeleton per Joomla 4: apri la gallery o una modal
(function(window, document) {
    'use strict';

    window.openArticleGallery = function() {
        // TODO: sostituisci con la tua implementazione (apri una modal custom / view / chiamata API)
        // Esempio minimo:
        if (window.bootstrap && window.bootstrap.Modal) {
            // Potresti creare una modal dinamica usando Bootstrap 5 se necessario
            alert('Apri la gallery (sostituisci con la tua implementazione).');
        } else {
            alert('Apri la gallery (sostituisci con la tua implementazione).');
        }
    };
})(window, document);